# ToyC Compiler 项目完整分析与学习计划

---

## 一、项目概览

ToyC Compiler 是一个**类 C 语言编译器**，将自定义的 C-like 语言源代码编译为 **RISC-V 汇编代码**。整个项目约 **1800 行代码**，架构清晰，适合系统学习编译器前端和后端的核心概念。

## 二、项目目录结构

```
toyc_compiler18-main/
├── CMakeLists.txt              # CMake 构建配置（C++20, Flex, Bison）
├── README.md                   # 项目说明文档
├── build.sh                    # 一键构建脚本
├── run_tests.sh                # 测试套件运行脚本
├── riscv.cpp                   # ⚠️ 根目录下的重复文件（与 src/codegen/riscv.cpp 相同，冗余）
├── src/                        # 源代码主目录
│   ├── main.cpp                # 程序入口，编排编译流水线
│   ├── lexer.l                 # Flex 词法分析器定义
│   ├── parser.y                # Bison 语法分析器定义
│   ├── ast/                    # 抽象语法树模块
│   │   ├── ast.hpp             #   AST 节点类定义（15 个节点类型 + Visitor 接口）
│   │   └── ast.cpp             #   AST 节点 accept/print 方法实现
│   ├── common/                 # 公共类型模块
│   │   └── types.hpp           #   FunctionInfo 结构体定义
│   ├── semantic/               # 语义分析模块
│   │   ├── analyzer.hpp        #   Scope + SemanticAnalyzer 类定义
│   │   └── analyzer.cpp        #   语义检查实现（Visitor 模式遍历 AST）
│   ├── codegen/                # 代码生成模块
│   │   ├── riscv.hpp           #   RISCVCodeGenerator 类定义
│   │   └── riscv.cpp           #   RISC-V 汇编代码生成实现（Visitor 模式遍历 AST）
│   └── utils/                  # 工具模块
│       ├── utils.hpp           #   工具函数声明（文件、路径、颜色、统计、Timer）
│       └── utils.cpp           #   工具函数实现
└── test_samples/               # 测试用例（17 个 .tc 文件）
    ├── 01_minimal.tc           #   最小程序：return 0
    ├── 02_assignment.tc        #   赋值语句
    ├── 03_if_else.tc           #   if-else 分支
    ├── 04_while_break.tc       #   while 循环 + break
    ├── 05_function_call.tc     #   函数调用
    ├── 06_continue.tc          #   continue 语句
    ├── 07_scope_shadow.tc      #   变量遮蔽
    ├── 08_short_circuit.tc     #   短路求值
    ├── 09_recursion.tc         #   递归函数（阶乘）
    ├── 10_void_fn.tc           #   void 函数
    ├── 11_precedence.tc        #   运算符优先级
    ├── 12_division_check.tc    #   除法运算
    ├── 13_scope_block.tc       #   块作用域
    ├── 14_nested_if_while.tc   #   嵌套 if + while
    ├── 15_multiple_return_paths.tc  # 多返回路径
    ├── 16_complex_syntax.tc    #   复杂语法综合
    └── 19_many_arguments.tc    #   多参数函数
```

## 三、编译流水线（Pipeline）

整个编译过程分 **4 个阶段**，在 [src/main.cpp](src/main.cpp) 中按序编排：

```
源代码(.tc) → [词法分析] → Token流 → [语法分析] → AST → [语义分析] → 标注后的AST → [代码生成] → RISC-V汇编(.s)
```

### 阶段1: 词法分析（Lexer）— [src/lexer.l](src/lexer.l)

- 使用 **Flex** 生成词法分析器
- 识别内容：
  - **关键字**：`int`, `void`, `if`, `else`, `while`, `break`, `continue`, `return`
  - **运算符**：`+`, `-`, `*`, `/`, `%`, `=`, `==`, `!=`, `<`, `<=`, `>`, `>=`, `&&`, `||`, `!`
  - **分隔符**：`(`, `)`, `{`, `}`, `,`, `;`
  - **标识符**：`[a-zA-Z_][a-zA-Z0-9_]*`
  - **数字字面量**：`0 | [1-9][0-9]*`
  - **注释**：`//` 单行注释, `/* */` 多行注释
  - **空白**：空格、制表符、换行符（被忽略）

### 阶段2: 语法分析（Parser）— [src/parser.y](src/parser.y)

- 使用 **Bison** 生成 LALR(1) 解析器
- 定义完整的 C-like 语言文法，包含 **12 级运算符优先级**（从低到高）：

| 优先级 | 运算符 | 结合性 |
|--------|--------|--------|
| 最低 | `\|\|` (OR) | 左结合 |
| | `&&` (AND) | 左结合 |
| | `==`, `!=` | 左结合 |
| | `<`, `<=`, `>`, `>=` | 左结合 |
| | `+`, `-` | 左结合 |
| 最高 | `*`, `/`, `%` | 左结合 |
| | `!`, 一元 `+`, 一元 `-` | **右结合** |

- 语法规则覆盖：
  - **程序结构**：`CompUnit → FuncDef | CompUnit FuncDef`
  - **函数定义**：`Type ID ( ParamList? ) Block`
  - **语句**：Block, 变量声明, 赋值, if/if-else, while, break, continue, return, 表达式语句
  - **表达式**：二元运算、一元运算、标识符、数字、括号、函数调用
- 解决了 **dangling-else** 歧义（`%prec NO_ELSE`）
- **输出**：构建出一棵 AST 树，根节点存储在全局变量 `root`（`unique_ptr<CompilationUnit>`）

### 阶段3: 语义分析（Semantic Analyzer）— [src/semantic/analyzer.cpp](src/semantic/analyzer.cpp)

- 采用 **Visitor 模式**遍历 AST
- 核心数据结构：
  - **Scope**（作用域管理）：用 `vector<unordered_map<string, Symbol>>` 实现作用域栈，支持嵌套作用域的 enter/exit
  - **Symbol**：记录变量名、类型、栈偏移、是否为参数
- 检查项：
  - `main` 函数是否存在且签名为 `int main()`
  - 变量/函数是否重复声明
  - 变量/函数使用前是否已声明
  - 函数调用实参数量与形参是否匹配
  - `break`/`continue` 是否在循环内
  - 函数返回值与声明类型是否匹配（void 不返回值，int 必须返回值）
  - 函数是否有返回语句

### 阶段4: 代码生成（Code Generator）— [src/codegen/riscv.cpp](src/codegen/riscv.cpp)

- 采用 **Visitor 模式**遍历 AST，生成 RISC-V 32 位汇编
- **RISC-V 调用约定**：前 8 个参数通过 `a0-a7` 传递，其余入栈；返回值在 `a0`
- **栈帧布局**：
  - `ra`（返回地址）保存在 `12(sp)`
  - `fp`（帧指针）保存在 `8(sp)`
  - 局部变量通过 `fp` 负偏移访问
- **控制流实现**：
  - `if-else`：条件求值 → `beqz` 跳转到 else → then 块 → `j` 到 end
  - `while`：loop 标签 → 条件求值 → `beqz` 到 end → 循环体 → `j` 到 loop
  - `break/continue`：通过 `breakLabels` / `continueLabels` 栈管理
- **表达式求值**：使用栈机模型，临时值在栈上分配（`sp -= 4`）
- **优化模块**（通过 `-opt` 标志启用）：
  - **常量折叠**：编译期计算常量表达式（如 `3 + 4` → `7`）
  - **代数简化**：`x + 0` → `x`, `x * 1` → `x`, `x * 0` → `0`
  - 死代码消除框架（`deadCodeElimination`，目前为占位实现）

## 四、关键设计模式

| 模式 | 应用位置 | 作用 |
|------|---------|------|
| **Visitor（访问者）** | AST 节点 + SemanticAnalyzer + RISCVCodeGenerator | 将 AST 结构遍历与操作解耦 |
| **RAII 智能指针** | 全局使用 `std::unique_ptr` | 自动内存管理，避免手动 delete |
| **栈式作用域** | Scope 类, RISCVCodeGenerator 的 scopeStack | 进入/退出作用域时保存/恢复符号表 |

## 五、支持的语言特性一览

- **类型系统**：`int`, `void`
- **变量**：声明 + 初始化 `int x = 10;`
- **算术**：`+`, `-`, `*`, `/`, `%`
- **比较**：`==`, `!=`, `<`, `<=`, `>`, `>=`
- **逻辑**：`&&`, `||`, `!`
- **控制流**：`if`, `if-else`, `while`
- **跳转**：`break`, `continue`, `return`
- **函数**：多参数、递归、void 函数
- **注释**：`//` 和 `/* */`
- **作用域**：`{}` 块级作用域，变量遮蔽
- **优化**：常量折叠、代数简化

## 六、注意事项/已知问题

1. **根目录 `riscv.cpp`** 与 `src/codegen/riscv.cpp` 是重复文件（内容完全一致），属于冗余。
2. **测试用例编号跳跃**（13→14→15→16→19），缺少 17、18 号测试。
3. **优化模式下的 bug**：[src/codegen/riscv.cpp:91-96](src/codegen/riscv.cpp#L91-L96) 中 `visit(BinaryExpression)` 调用 `optimizeConstantFolding(node)` 后无条件 `return`，导致无法折叠的二元表达式在 `-opt` 模式下不生成任何代码。

---

## 七、学习计划

### 第一阶段：环境搭建与运行体验（预计 0.5 天）

- [ ] 安装依赖：CMake, Flex, Bison, GCC（Windows 建议用 MSYS2 或 WSL）
- [ ] 运行 `./build.sh` 构建项目
- [ ] 用测试用例运行编译器：`./build/toyc < test_samples/01_minimal.tc`
- [ ] 观察生成的 RISC-V 汇编输出，对照 `.tc` 源文件理解对应关系
- [ ] 尝试 `-opt` 优化模式

### 第二阶段：理解 AST 与访问者模式（预计 1 天）

- [ ] 阅读 [src/ast/ast.hpp](src/ast/ast.hpp)，画出类继承图（ASTNode → Expression / Statement → 各具体节点）
- [ ] 理解 **Visitor 模式**：为什么编译器需要它？如何实现双重分派？
- [ ] 阅读 [src/ast/ast.cpp](src/ast/ast.cpp) 中的 `accept()` 和 `print()` 方法
- [ ] 手动构造一棵简单的 AST 并调用 `print()` 观察输出

### 第三阶段：词法分析与语法分析（预计 2 天）

- [ ] 学习 Flex 基础：正则表达式 → Token 的映射规则
- [ ] 阅读 [src/lexer.l](src/lexer.l)，理解每个 Token 的识别逻辑
- [ ] 学习 Bison 基础：BNF 文法、`%union`、`%type`、优先级声明
- [ ] 阅读 [src/parser.y](src/parser.y)，理解**运算符优先级**如何通过 `%left`/`%right` + 递归下降文法实现
- [ ] 理解**文法动作（semantic actions）**：如何从 Token 逐步构建 AST 节点
- [ ] 重点理解：`dangling-else` 问题的解决方案（`%prec NO_ELSE`）

### 第四阶段：语义分析（预计 1.5 天）

- [ ] 阅读 [src/semantic/analyzer.hpp](src/semantic/analyzer.hpp)，理解作用域栈的实现原理
- [ ] 阅读 [src/semantic/analyzer.cpp](src/semantic/analyzer.cpp)，逐方法理解每个 `visit()` 做了什么语义检查
- [ ] 理解**符号表**（Symbol Table）的作用和实现
- [ ] 尝试构造有语义错误的 `.tc` 文件，验证编译器是否能检测

### 第五阶段：代码生成（预计 2 天）

- [ ] 学习 RISC-V 基础指令集：`li`, `add`, `sub`, `mul`, `div`, `rem`, `lw`, `sw`, `beqz`, `j`, `ret`
- [ ] 理解 RISC-V 调用约定（calling convention）：`a0-a7` 传参，`a0` 返回值，`ra` 返回地址
- [ ] 阅读 [src/codegen/riscv.cpp](src/codegen/riscv.cpp)，理解栈帧管理、表达式求值、控制流、函数调用
- [ ] 阅读**常量折叠**优化代码，理解编译期优化的基本思路

### 第六阶段：综合实践（预计 1-2 天）

- [ ] 修复优化模式下 `visit(BinaryExpression)` 的 bug
- [ ] 删除根目录冗余的 `riscv.cpp`
- [ ] 尝试扩展功能（任选其一）：
  - 添加 `for` 循环支持
  - 实现真正的死代码消除
  - 添加数组类型支持
  - 添加简单的寄存器分配优化
- [ ] 将 RISC-V 汇编在 RARS 模拟器上实际运行验证
